const Counter = ({addCount}) => {
    
    return (
        <>
        <button onClick = {addCount}>
            Click me to increase count
        </button>
        </>
    )
}

export default Counter